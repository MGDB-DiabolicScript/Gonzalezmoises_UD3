using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//public class gonzalezmoises@cuaderno : MonoBehaviour{
    // Start is called before the first frame update
    //void Start(){}

    // Update is called once per frame
    //void Update(){}}

// propiedades de la esfera
//El componente Transformar determina la posición, la rotación y la escala de cada objeto en la escena.
//Rotacion:permite mover por los 3 ejes el objeto.
//Escala:El valor "1" es el tamaño original, Nos permite cambiar el tamaño del objeto.
//Posicion:permite mover el objeto puesto en escena atra vez de los ejes X,Y,Z.
//addforce:permite dar mayor fuerza de rigidbody haciendo que coja velocidad o se relentice